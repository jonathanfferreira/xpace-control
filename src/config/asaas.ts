// IMPORTANT: These are SANDBOX credentials for development only
// Production credentials should be stored securely in edge function secrets
export const ASAAS_SANDBOX_CONFIG = {
  apiUrl: 'https://sandbox.asaas.com/api/v3',
  // API key should be set by each school in their settings
  // This is a placeholder - users must configure their own Asaas sandbox key
  apiKey: '', // Will be loaded from school settings
};
